# 🧠 Memória Viva do Estudo de Arquitetura (MEMORIA.md)
**Projeto:** Plataforma de Sistemas Inteligentes (Padrão PycoderBR / SCSI)

---

## 📌 Origem e Propósito
- **Referência:** Curso de Arquitetura de Deploy de Sistemas Inteligentes da PycoderBR Treinamentos (SCSI - Sistema de Gestão de Corretora de Seguros Inteligente).
- **Objetivo Central:** Servir como base e fundação padronizada para o desenvolvimento, orquestração e deploy de todos os sistemas da equipe (incluindo modernização de processos, relatórios e automações).

---

## 📌 Premissas e Decisões Técnicas Consolidadas

### 1. Desacoplamento entre Web e Processamento Pesado
- **Decisão:** A camada web (Django + Gunicorn) NUNCA deve executar chamadas síncronas demoradas (como LLMs ou relatórios volumosos).
- **Implementação:** Toda requisição que demorar mais de 500ms é despachada para filas do **RabbitMQ** e executada por **Celery Workers**, com status consultado no **Redis**.

### 2. Separação de Funções entre RabbitMQ e Redis
- **RabbitMQ:** Responsável exclusivo pelo transporte seguro de mensagens e gerenciamento de filas (Broker).
- **Redis:** Responsável exclusivo pelo cache de alta velocidade, sessões web do Django e armazenamento temporário de resultados do Celery (*Results Backend*).

### 3. Roteamento Dinâmico com Traefik
- **Decisão:** Utilizar Traefik em vez de Nginx estático para gerenciar ingress e certificados SSL na VPS.
- **Motivo:** O Traefik descobre novos containers automaticamente através de labels no Docker, simplificando deploys e renovação de SSL sem necessidade de recarregar arquivos de configuração manualmente.

### 4. Armazenamento e Banco de Dados
- **PostgreSQL 16+:** Banco relacional primário com dados em volume Docker persistente.
- **Habilitação de pgvector:** Planejada para armazenamento de embeddings vetoriais para os agentes de LangGraph.

### 5. Operação API-First & Agente Autônomo Headless (ADR 011)
- **Decisão:** Toda e qualquer ferramenta, serviço em nuvem ou componente da stack técnica (Cloudflare, GitHub, Hostinger VPS, Traefik Ingress, PostgreSQL 16, Redis, RabbitMQ, Celery e APIs de IA) deve ser operado, auditado, monitorado e provisionado pelo Antigravity **diretamente via API oficial, CLI ou SDK programático**.
- **Benefício Operacional:** O operador humano comanda a arquitetura via linguagem natural no chat e é dispensado de abrir dashboards gráficos, navegar em consoles web ou realizar tarefas manuais repetitivas.
- **Segurança & Soberania:** Credenciais, tokens e segredos de API residem estritamente no arquivo local `.env` (protegido pela linha 27 do `.gitignore`), garantindo acesso de menor privilégio e zero vazamento para o Git.

---

## 📌 Histórico de Evolução do Estudo
- **[21/09/2026]:** Criação oficial do diretório de estudo estudo_arquitetura_sistemas_pycoder.
- **[21/09/2026]:** Inclusão do diagrama oficial da arquitetura SCSI em docs/arquitetura_deploy_scsi.png.
- **[21/09/2026]:** Elaboração do README.md, ARQUITETURA.md, AGENTS.md e MEMORIA.md.
- **[21/09/2026 - Passo 1.1.1]:** Diagnóstico do ecossistema Python concluído: Python 3.14.7 (64-bit) ativo no host, módulo venv nativo funcional, Astral uv/uvx v0.12.7 instalado e operacional. **Decisão (ADR):** Adotado Python 3.12 gerenciado hermeticamente pelo `uv` para o projeto, garantindo paridade 1:1 com os containers Docker e compatibilidade total de bibliotecas.
- **[21/09/2026 - Passo 1.1.2]:** Diagnóstico de virtualização e containers no Windows: Docker Desktop e WSL2 ausentes no Windows. Hardware auditado possui VT-x habilitado e 7.15GB de RAM livre. **Decisão (ADR 006):** Adotada formalmente a abordagem **Cloud-Native / VPS-First**, utilizando o Windows como estação ágil de desenvolvimento e a VPS Hostinger Ubuntu Linux para a orquestração completa dos containers Docker Swarm.
- **[21/09/2026 - Passo 1.1.3]:** Diagnóstico e ajustes de Git e Versionamento concluídos: Git 2.55.0 ativo, GCM ativo para autenticação HTTPS. **Ajustes aplicados:** Removido o espaço em branco inicial do e-mail global (`marcos.vinicius2323@gmail.com`), associando perfeitamente a autoria ao GitHub; padronizado `init.defaultBranch = main` para eliminar descompasso de branches. `core.autocrlf = true` mantido com recomendação de `.gitattributes` para scripts Linux.
- **[21/09/2026 - Passo 1.1.4]:** Diagnóstico e ajustes do Shell e Permissões concluídos: Windows PowerShell 5.1 (Build 26100), `LongPathsEnabled = 1` ativo no Registro do Windows (sem limite de 260 caracteres para árvores profundas de IA). **Ajuste aplicado:** Configurada explicitamente a `ExecutionPolicy` do escopo `CurrentUser` para `RemoteSigned`, garantindo que scripts locais (como `activate.ps1`) rodem sem bloqueios mesmo em sessões não-elevadas. **Diretriz técnica:** Adotado modo nativo do `uv` (hardlinks/copies) para criação do virtualenv, dispensando links simbólicos que exigiriam elevação de Administrador.
- **[21/09/2026 - Passo 1.1.5]:** **Conclusão formal do Passo 1.1 (Host Readiness Report):** Todos os pilares do ambiente host foram auditados, calibrados e aprovados. **Ajuste aplicado:** Configurada a variável persistente `PYTHONUTF8 = 1` no escopo do usuário (PEP 540), garantindo operação pura em UTF-8 no Windows e eliminando erros de acentuação/encoding. Confirmada a presença do cliente nativo **OpenSSH 9.5p2**, pronto para conexões com a VPS Hostinger sem emuladores externos. Ambiente 100% pronto para o Passo 1.2.
- **[21/09/2026 - Passo 1.2.1]:** Criado o arquivo `.python-version` na raiz do projeto com o valor `3.12`. O `uv` agora ignora automaticamente o Python 3.14 do sistema e passa a buscar exclusivamente o CPython 3.12 hermético para todas as operações locais.
- **[21/09/2026 - Passo 1.2.2]:** Executado `uv python install 3.12`. Provisionado com sucesso o CPython 3.12.14 (64-bit) de forma hermética em `AppData\Roaming\uv\python`, sem alterar o PATH do Windows ou o Python 3.14 do sistema. `uv python find` resolvido para o novo runtime com sucesso.
- **[21/09/2026 - Passo 1.2.3]:** Executado `uv venv .venv`. Criado o ambiente virtual isolado vinculado exclusivamente ao CPython 3.12.14. `include-system-site-packages = false` assegura isolamento total contra pacotes globais do Windows. Gerados os scripts de ativação (`activate.ps1`, `activate.bat`, `activate`).
- **[21/09/2026 - Passo 1.2.4]:** Smoke Test do interpretador executado com sucesso: `sys.version` confirmou CPython 3.12.14, `sys.executable` e `sys.prefix` confirmaram isolamento hermético dentro de `.venv`, script `activate.ps1` ativou a variável `$env:VIRTUAL_ENV` perfeitamente no PowerShell, e `uv run` executou scripts usando o ambiente sem necessidade de ativação manual.
- **[21/09/2026 - Passo 1.2.5]:** **Conclusão formal do Passo 1.2:** Criado o `.gitignore` corporativo preventivo blindando a pasta `.venv/`, caches do Python/Ruff e segredos `.env`. O ambiente virtual está 100% isolado, funcional e protegido contra vazamento no Git. Passo 1.2 formalmente homologado.
- **[21/09/2026 - Passo 1.3.1]:** Instalação das ferramentas de desenvolvimento e qualidade (`ruff`, `pytest`, `pytest-cov`) concluída com sucesso via `uv pip install`. Instalados `ruff 0.16.8`, `pytest 9.1.1` e `pytest-cov 7.1.0` de forma estritamente isolada no `.venv`. Binários operacionais e verificados via `uv run`.
- **[21/09/2026 - Passo 1.3.2]:** Criação e estruturação do `pyproject.toml` (PEP 518, PEP 621, PEP 735) estabelecendo os metadados do projeto, restrição formal de runtime `requires-python = ">=3.12,<3.13"`, grupo de dependências de desenvolvimento (`[dependency-groups] dev`) e configuração de projeto de aplicação (`[tool.uv] package = false`). Gerado o lockfile hermético `uv.lock` com hashes criptográficos multiplataforma.
- **[21/09/2026 - Passo 1.3.3]:** Configuração aprofundada do Ruff no `pyproject.toml` (`[tool.ruff]`, `[tool.ruff.lint]`, `[tool.ruff.format]`, `[tool.ruff.lint.isort]`). Definido `target-version = "py312"`, comprimento de linha 88, regras rigorosas de linting (`E`, `W`, `F`, `I`, `B`, `C4`, `UP`, `ARG`, `SIM`), formatação determinística e imports de primeira classe organizados (`core`, `apps`, `config`). Teste `uv run ruff check .` executado com 100% de sucesso.
- **[21/09/2026 - Passo 1.3.4]:** Configuração do Pytest e Cobertura de Código no `pyproject.toml` (`[tool.pytest.ini_options]`, `[tool.coverage.run]`, `[tool.coverage.report]`). Criado o diretório `tests/` e a suíte inicial de sanidade `test_harness.py` cobrindo versão 3.12, isolamento do virtualenv e recursos modernos (PEP 695 type aliases e pattern matching). Execução via `uv run pytest` concluída com 3/3 testes aprovados em 0.04s.
- **[21/09/2026 - Passo 1.3.5]:** **Conclusão formal do Passo 1.3:** Smoke test integrado do pipeline de qualidade executado com 100% de sucesso (`uv run ruff check .` sem erros, 5 arquivos formatados, 3/3 testes passando em 0.03s). Gerado e salvo o relatório executivo em PDF de alta resolução no Desktop (`Passo_1.3_Linter_Formatter_e_Testes.pdf`, 211 KB) e em `docs/`. Passo 1.3 formalmente homologado.
- **[21/09/2026 - Passo 1.4.1]:** Criação do arquivo `.editorconfig` na raiz do projeto com regras universais de formatação para editores/IDEs: charset `utf-8`, quebra de linha `lf`, remoção de espaços finais, 4 espaços para Python (alinhado a 88 colunas) e 2 espaços para configurações estruturadas (YAML, JSON, TOML, Docker).
- **[21/09/2026 - Passo 1.4.2]:** Criação do arquivo `.gitattributes` na raiz do projeto implementando a ADR 007 (blindagem de line endings para containers Linux). Fixado `*.sh text eol=lf`, `Dockerfile* text eol=lf` e `*.py text eol=lf` para impedir que o `core.autocrlf = true` do Windows contamine scripts executados na VPS Hostinger com caracteres `\r`. Mapeados arquivos binários e normalização global `* text=auto`.
- **[21/09/2026 - Passo 1.4.3]:** Criação do arquivo `.env.example` corporativo como contrato declarativo de variáveis de ambiente cobrindo as 7 camadas da stack SCSI: Django Core, PostgreSQL 16, Redis Cache, RabbitMQ Broker, Celery Worker/Beat, APIs de IA (OpenAI, Gemini, Anthropic, LangSmith) e Borda Ingress (Cloudflare e Traefik).
- **[21/09/2026 - Passo 1.4.4]:** Auditoria integrada de conformidade dos guardrails: verificação de encoding (100% UTF-8 sem BOM em todos os 6 arquivos de governança), confirmação de quebra de linha LF (Unix) pura, validação do padrão `.gitignore` blindando arquivos de segredo locais (`.env`, `.env.local`) e liberando `.env.example`, e execução limpa do pipeline de linter e testes.
- **[21/09/2026 - Passo 1.4.5]:** **Conclusão formal do Passo 1.4:** Consolidação dos guardrails arquiteturais do repositório (`.editorconfig`, `.gitattributes` ADR 007, `.env.example`). Gerado e salvo o relatório executivo em PDF de alta resolução no Desktop (`Passo_1.4_Governanca_e_Guardrails.pdf`, 277 KB) e em `docs/`. Passo 1.4 formalmente homologado.
- **[21/09/2026 - Passo 1.5.1]:** Auditoria dos servidores MCP (Model Context Protocol) concluída com sucesso. Mapeados e catalogados os 3 servidores ativos: `notebooklm` (20 ferramentas para RAG, sessões de pesquisa e geração de áudio explicativo), `postgres` (ferramenta `query` para inspeção relacional e pgvector) e `puppeteer` (7 ferramentas para testes E2E, capturas visuais e auditoria de páginas renderizadas).
- **[21/09/2026 - Passo 1.5.2]:** Validação das políticas de terminal e ambiente concluída: confirmada `ExecutionPolicy RemoteSigned` (CurrentUser), cliente OpenSSH 9.5p2 ativo para conexões com a VPS, e aprimorado o script `Activate.ps1` com export automático de `PYTHONUTF8 = 1` na ativação (e limpeza no `deactivate`). Teste de ativação comprovou `UTF8_MODE=1` e `STDOUT=utf-8` nativo.
- **[21/09/2026 - Passo 1.5.3]:** Auditoria e alinhamento dos 4 subagentes especialistas de `AGENTS.md`: corrigido o identificador canônico de `arquiteto_solucoes` e estabelecida a Matriz de Atuação Operacional cruzando os papéis dos especialistas (`arquiteto_solucoes`, `engenheiro_devops`, `engenheiro_backend`, `engenheiro_ia`) com as 7 fases estratégicas da arquitetura SCSI.
- **[21/09/2026 - Passo 1.5.4]:** Smoke test integrado e consolidado de toda a Fase 1 executado com 100% de sucesso: Python 3.12.14 isolado em `.venv`, Ruff sem erros estáticos e com formatação perfeita, Pytest com 3/3 testes aprovados em 0.03s, `uv sync` em 1ms, 11/11 arquivos de governança verificados e zero arquivos residuais/temporários no workspace.
- **[21/09/2026 - Passo 1.5.5]:** **Conclusão formal do Passo 1.5 & HOMOLOGAÇÃO DA FASE 1:** Emissão do relatório executivo em PDF de alta resolução no Desktop (`Passo_1.5_Antigravity_e_Homologacao_Fase1.pdf`, 211 KB) e em `docs/`. A **FASE 1 (Harness do Antigravity 2.0 & Ambiente de Desenvolvimento Local)** está 100% concluída, documentada e aprovada por unanimidade pelos 4 subagentes especialistas, liberando formalmente o início da **Fase 2 (Git & GitHub)**.
- **[21/09/2026 - Micro-Fase 2.1.1]:** Repositório Git local inicializado formalmente com a branch canônica `main` (`git init -b main`). Criada a estrutura de dados `.git/` (hooks, info, objects, refs, config, HEAD), verificado `git rev-parse --is-inside-work-tree = true` e comprovado que o ponteiro de branch inicial aponta para `refs/heads/main` sem resquícios do padrão legado `master`.
- **[21/09/2026 - Micro-Fase 2.1.2]:** Calibração de governança local do Git concluída: configurados no escopo local (.git/config) `core.longpaths = true` (eliminando o teto de 260 caracteres no Windows) e `init.defaultBranch = main`. Confirmada a herança estrita dos metadados globais de autoria (`user.name = mteixeira23`, `user.email = marcos.vinicius2323@gmail.com`) e comportamento de checkout `core.autocrlf = true` protegido pela ADR 007.
- **[21/09/2026 - Micro-Fase 2.1.3]:** Auditoria matemática do motor de exclusão (.gitignore) concluída via `git check-ignore -v`. Comprovado o bloqueio estrito de `.venv/` (linha 6), `.coverage` (linha 22), `.pytest_cache/` (linha 20), `.ruff_cache/` (linha 19), `.env` (linha 27), `.env.local` (linha 28) e arquivos temporários (linha 44). Confirmada a isenção intencional de `!.env.example` (linha 29) e a elegibilidade plena dos arquivos de governança e código para commit.
- **[21/09/2026 - Micro-Fase 2.1.4]:** Auditoria do motor de atributos (.gitattributes) concluída via `git check-attr -a`. Confirmada a aplicação estrita da ADR 007 (`eol: lf` e `text: set`) para scripts (`*.sh`), arquivos Docker (`Dockerfile*`, `docker-compose.yml`), Python (`*.py`) e configurações (`pyproject.toml`), além do tratamento inviolável de binários (`binary: set`, `diff: unset`, `text: unset`) para arquivos `.png` e `.pdf`.
- **[21/09/2026 - Micro-Fase 2.1.5]:** **Conclusão formal da Micro-Fase 2.1:** Inspeção do estado de trabalho via `git status` e `git status -u` concluída com 100% de conformidade. Repositório na branch `main`, sem commits prévios (`No commits yet`). Mapeados com precisão 24 itens legítimos elegíveis para o staging (governança, documentação, testes, scripts de PDF e lockfile). Confirmada a ausência absoluta de `.venv/`, `.coverage` ou caches no tracking. Micro-fase 2.1 formalmente concluída e homologada.
- **[21/09/2026 - Micro-Fase 2.2.1]:** Empacotamento cirúrgico para a Staging Area concluído com sucesso via `git add .`. Todos os 24 itens legítimos migraram para a Staging Area (`Changes to be committed`), sem nenhuma contaminação de `.venv/`, `.coverage` ou caches. Avisos de normalização de quebra de linha confirmaram a substituição de CRLF por LF no banco de objetos para arquivos de governança, em perfeita conformidade com a ADR 007.
- **[21/09/2026 - Micro-Fase 2.2.2]:** Auditoria estatística e de blobs na Staging Area concluída (`git diff --cached --stat` e `git ls-files --stage`). Confirmada a indexação de 25 arquivos (3.155 inserções de texto e 7 binários invioláveis). Inspeção byte a byte do banco de objetos do Git comprovou ausência total do byte 13 (`\r` CR) nos blobs de texto, validando a pureza estrita de LF (Unix) nos objetos internos (ADR 007).
- **[21/09/2026 - Micro-Fase 2.2.3]:** Formulação da mensagem do commit inicial estruturada no padrão Conventional Commits (`chore(foundation): inicializacao do harness de qualidade e governanca do projeto scsi`). Documentados escopo, rastreabilidade de ADRs (ADRs 005 a 009), autoria de `mteixeira23` e branch `main`. Mensagem encapsulada em `scripts/.commit_msg.tmp` em UTF-8 puro e blindada pelo `.gitignore` (*.tmp).
- **[21/09/2026 - Micro-Fase 2.2.4]:** Execução física do primeiro commit formal do repositório (`root-commit`) na branch `main` via `git commit -F scripts/.commit_msg.tmp`. Gerado o hash criptográfico `0430a12586052547fcacf5dbb9ac2afdaba76274` (`0430a12`). Criados 25 arquivos com permissão POSIX 100644 (3.157 inserções). Saneado e removido o buffer temporário de mensagem sem resíduos.
- **[21/09/2026 - Micro-Fase 2.2.5]:** **Conclusão formal da Micro-Fase 2.2:** Auditoria pós-commit realizada com sucesso via `git log -1 --pretty=fuller --stat` e `git status`. Confirmada a autoria e comitente de `mteixeira23 <marcos.vinicius2323@gmail.com>`, integridade dos 25 arquivos e estado da árvore de trabalho 100% limpo (`nothing to commit, working tree clean`). Micro-fase 2.2 formalmente concluída e homologada.
- **[21/09/2026 - Micro-Fase 2.3.1]:** Diagnóstico de transporte e autenticação remota concluído. Confirmado Git Credential Manager ativo (`credential.helper = manager`), biblioteca criptográfica TLS nativa do Windows (`http.sslbackend = schannel`) e conectividade TCP 100% estabelecida na porta 443 com o cluster do GitHub (4.228.31.150). Adotado formalmente o transporte HTTPS autenticado como padrão corporativo para a conexão com o repositório remoto.
- **[21/09/2026 - Micro-Fase 2.3.2]:** Definição e validação do endpoint remoto no GitHub concluída. Confirmada a existência e autenticidade da conta `mteixeira23` (HTTP 200). Fixada a URL canônica `https://github.com/mteixeira23/estudo_arquitetura_sistemas_pycoder.git` como target oficial do remote `origin`. Documentadas as diretrizes de criação como repositório vazio (sem README/gitignore redundantes) para o vínculo do upstream.
- **[21/09/2026 - Micro-Fase 2.3.3]:** Configuração do remote `origin` concluída com sucesso via `git remote add origin https://github.com/mteixeira23/estudo_arquitetura_sistemas_pycoder.git`. Inspecionado `.git/config` e comprovada a criação da seção `[remote "origin"]` com refspec `+refs/heads/*:refs/remotes/origin/*` e URLs canônicas ativas para fetch e push.
- **[21/09/2026 - Micro-Fase 2.3.4]:** Teste de conectividade e handshake criptográfico executado via `git ls-remote origin`. O cluster do GitHub respondeu com sucesso ao handshake HTTPS/TLS (`remote: Repository not found`), comprovando conectividade perfeita e indicando que o repositório remoto está aguardando criação na interface web do GitHub (github.com/new) como repositório vazio.
- **[21/09/2026 - Micro-Fase 2.3.5]:** **Conclusão formal da Micro-Fase 2.3:** Consolidação da topologia remota concluída com sucesso. Remote `origin` canônico estabelecido na branch `main`, com handshake TLS atestado e rastreabilidade total configurada. Repositório local 100% pronto para receber a esteira de CI do GitHub Actions (Micro-fase 2.4) e o primeiro push (Micro-fase 2.5).
- **[21/09/2026 - Micro-Fase 2.4.1]:** Estruturação do diretório corporativo de workflows (`.github/workflows/`) concluída na raiz do repositório. Validação via `git check-ignore` confirmou que a pasta é 100% elegível para versionamento e governada pelo Git.
- **[21/09/2026 - Micro-Fase 2.4.2]:** Elaboração declarativa do workflow corporativo de CI (`.github/workflows/ci.yml`) concluída. Configurados triggers em `push`, `pull_request` (branch `main`) e `workflow_dispatch`, runner nativo `ubuntu-latest`, action oficial `astral-sh/setup-uv@v5` com cache ativado via `uv.lock`, sincronização hermética `uv sync --frozen` e execução dos quality gates (`ruff check`, `ruff format --check` e `pytest -v`). Arquivo gerado com 2 espaços de indentação e LF estrito.
- **[21/09/2026 - Micro-Fase 2.4.3]:** Auditoria sintática e de formatação de `.github/workflows/ci.yml` concluída via script Python. Comprovada a ausência total de BOM (`has_bom = False`), ausência total de caracteres de retorno de carro (`\r` / CR = False - ADR 007), ausência total de TABs (`\t` = False) e 100% das 45 linhas aderentes à indentação estrita de 2 espaços (`.editorconfig`).
- **[21/09/2026 - Micro-Fase 2.4.4]:** Empacotamento e commit semântico da esteira de CI concluídos. Adicionado `.github/workflows/ci.yml` e `MEMORIA.md` à Staging Area e registrado o commit `ci(github): configurar esteira de integracao continua com astral uv, ruff e pytest`, garantindo versionamento semântico do pipeline e working tree 100% limpa.
- **[21/09/2026 - Micro-Fase 2.5.1]:** Instanciação automatizada e segura do repositório remoto vazio (`bare`) via API oficial do GitHub (`https://github.com/mteixeira23/estudo_arquitetura_sistemas_pycoder`). Repositório configurado com sucesso sem arquivos concorrentes (`auto_init: false`), eliminando qualquer risco de divergência de históricos (`unrelated histories`). Conectividade confirmada via `git ls-remote origin`.
- **[21/09/2026 - Micro-Fase 2.5.2]:** Execução do primeiro push canônico do ecossistema SCSI via `git push -u origin main`. Sincronizados com sucesso os 5 commits da branch `main` e estabelecido o rastreamento canônico de upstream (`branch 'main' set up to track 'origin/main'`). Árvore de objetos transferida com 100% de integridade criptográfica.
- **[21/09/2026 - Micro-Fase 2.5.3]:** Auditoria em tempo real da primeira execução da esteira de Integração Contínua remota no GitHub Actions. Workflow disparado pelo evento de push na branch `main` (Run ID: `35653337944`). Concluído com status `success` em impressionantes **11 segundos** no runner Linux (`ubuntu-latest`). Todos os portões de qualidade (Setup Astral uv, CPython 3.12, Sync hermético do `uv.lock`, Ruff Linter, Ruff Formatter e Pytest com cobertura) aprovados com louvor.
- **[21/09/2026 - Micro-Fase 2.5.4]:** Compilação do relatório executivo de governança e homologação da Fase 2 em alta resolução. Gerado o documento canônico `Passo_2_Git_e_GitHub.pdf` (355 KB) via Edge Headless (`--headless=new --disable-gpu --no-pdf-header-footer`) e distribuído com sucesso nas pastas `C:\Users\marcos.teixeira\Desktop\Estudo Pycoder\` e `docs/`.
- **[21/09/2026 - Micro-Fase 2.5.5]:** **Conclusão formal e homologação da Fase 2 (Git & GitHub):** Todos os objetivos estratégicos do Passo 2 foram plenamente atingidos e homologados pelos 4 subagentes especialistas (`arquiteto_solucoes`, `engenheiro_devops`, `engenheiro_backend` e `engenheiro_ia`). Transição autorizada para a **Fase 3: Borda, Domínio & Criptografia** (Passo 3: Cloudflare e Passo 4: SSL/TLS Full Strict).
- **[21/09/2026 - Micro-Fase 3.1.1]:** Desenho da topologia de tráfego Anycast global da Cloudflare e modelo de Ingress de borda concluído. Elaborada a documentação canônica em `docs/topologia_borda_cloudflare.md` definindo o fluxo de triplo hop (Navegador -> Borda Cloudflare -> VPS Hostinger -> Traefik / Docker Swarm), os headers HTTP corporativos de rastreabilidade (`CF-Connecting-IP`, `X-Forwarded-Proto`, `CF-Ray`) e as regras arquiteturais para neutralização do vetor de ataque *Direct Origin Bypass*.
- **[21/09/2026 - Micro-Fase 3.1.2]:** Elaboração da Matriz Canônica de Subdomínios concluída. Mapeados e especificados em `docs/matriz_subdominios_scsi.md` os hosts canônicos: Apex `@` e `www` (Landing/Portal com redirect 301), `api` (Django Gunicorn com bypass de cache e suporte a streaming SSE), `app` (Console SPA com cache imutável de assets) e `traefik` (Dashboard protegido com BasicAuth e restrição IP/Zero Trust). Definidas as diretrizes de labels do Traefik para orquestração no Docker Swarm.
- **[21/09/2026 - Micro-Fase 3.1.3]:** Especificação da Tabela Declarativa de Registros DNS concluída. Documentados em `docs/tabela_registros_dns_cloudflare.md` e gerado o arquivo de zona BIND RFC 1035 em `docs/scsi.pycoder.com.br.zone`. Configurados registros Tipo A (`@` e `api`), CNAME (`www`, `app`, `traefik`, `flower`) com Proxy Laranja ativo (`proxied: true`), e registros TXT anti-spoofing/anti-phishing (SPF `v=spf1 -all` e DMARC `p=reject`). Mapeados payloads de automação REST v4 da Cloudflare.
- **[21/09/2026 - Micro-Fase 3.1.4]:** Políticas Operacionais do Proxy Laranja consolidadas em `docs/politicas_proxy_laranja_cloudflare.md`. Mapeadas as matrizes de portas HTTP/HTTPS da Cloudflare, padronizado o uso de WebSockets (`wss://`) para agentes de IA e estabelecido o checklist rigoroso de não-vazamento de IP real da VPS (*Origin IP Leakage Prevention*), com diretriz de proibir registros DNS públicos para SSH e adoção de túnel Zero Trust / chaves assimétricas Ed25519.
- **[21/09/2026 - ADR 011]:** **Formalização da Decisão Arquitetural ADR 011 (Paradigma API-First & Operação Headless pelo Agente Antigravity):** Estabelecido como mandato global que todas as ferramentas do ecossistema SCSI (Cloudflare, GitHub, Hostinger, Traefik, PostgreSQL, Redis, RabbitMQ, Celery, LLMs) devem ser operadas e auditadas diretamente pelo Antigravity via APIs oficiais e SDKs herméticos, sem necessidade de navegação manual pelo usuário em interfaces gráficas. Configurado e validado com sucesso o token da Cloudflare API (`CLOUDFLARE_API_TOKEN`) armazenado no cofre local `.env` (blindado pelo `.gitignore`), atestado como ativo (`This API Token is valid and active`) e pronto para inspeção autônoma de zonas, DNS, WAF e SSL.
- **[21/09/2026 - Sub-etapa 3.2.1]:** Especificação de Regras de WAF (Web Application Firewall) na borda concluída em `docs/regras_waf_cloudflare.md`. Mapeadas 4 regras canônicas na sintaxe Wirefilter (Cloudflare Expression Language): SCSI-WAF-01 (Bloqueio 403 de probes/scanners de .env, .git, WordPress e PHP), SCSI-WAF-02 (Mitigação 403 de ataques OWASP SQLi, XSS e Path Traversal com lower normalizado), SCSI-WAF-03 (Bloqueio 403 de métodos HTTP anômalos fora da lista padrão) e SCSI-WAF-04 (Desafio interativo Managed Challenge Turnstile para acessos administrativos em `/admin/` e `traefik.` com threat_score > 10). Mapeado payload declarativo JSON compatível com o endpoint oficial da Cloudflare API v4 Ruleset Engine (`http_request_firewall_custom`), aderente à ADR 011. Homologado por unanimidade (4/4).
- **[21/09/2026 - Sub-etapa 3.2.2]:** Especificação de Políticas de Rate Limiting na borda concluída em `docs/rate_limiting_cloudflare.md`. Mapeadas 4 políticas canônicas na fase `http_ratelimit` do Cloudflare Ruleset Engine: SCSI-RL-01 (Anti-Brute-Force em `/admin/login/` e `/api/v1/auth/login/` limitando a 5 req/min com bloqueio HTTP 429 por 15 minutos), SCSI-RL-02 (Blindagem de Endpoints de IA e LangGraph limitando a 10 req/min com bloqueio por 5 minutos para proteção de cotas), SCSI-RL-03 (Fair-Use da API REST em 60 req/min com Managed Challenge) e SCSI-RL-04 (Blindagem de dashboards Traefik/Flower em 20 req/min com Managed Challenge por 10 minutos). Mapeado payload JSON declarativo da API v4, formato de resposta RFC 6585 e coexistência em profundidade com Rate Limiting do Django/Redis (ADR 008). Homologado por unanimidade (4/4).
- **[21/09/2026 - Sub-etapa 3.2.3]:** Especificação de Mitigação Volumétrica DDoS e Modo "Under Attack" concluída em `docs/mitigacao_ddos_cloudflare.md`. Mapeada a proteção multi-camadas: L3/L4 (SYN Floods, UDP Amplification, ICMP) absorvidos pela malha Anycast global de 330+ Tbps sem alcance ao IP da VPS; e L7 (HTTP Floods, Slowloris, RUDY e HTTP/2 Rapid Reset) neutralizados pelo proxy reverso completo da Cloudflare (Full Reverse Proxy), que só encaminha requisições ao Traefik quando integralmente recebidas. Documentado o modo emergencial "Under Attack Mode" com desafio Turnstile transparente e scripts de automação headless via Cloudflare API v4 (`PATCH /zones/{zone_id}/settings/security_level`), em total conformidade com a ADR 011. Homologado por unanimidade (4/4).
- **[21/09/2026 - Sub-etapa 3.2.4]:** Especificação de Bot Management, Bloqueio de AI Scrapers e Filtros Geográficos/Reputação concluída em `docs/bot_management_scrapers_cloudflare.md`. Mapeadas a ativação do Browser Integrity Check (BIC) contra cabeçalhos anômalos e 3 políticas canônicas: SCSI-BOT-01 (Bloqueio 403 imediato de AI crawlers de treinamento como GPTBot, CCBot, Bytespider, ClaudeBot, PerplexityBot e Amazonbot para proteção de dados proprietários de apólices), SCSI-BOT-02 (Bloqueio 403 de bibliotecas de raspagem direta como python-requests, aiohttp e Scrapy em rotas SPA) e SCSI-BOT-03 (Desafio Managed Challenge em `/admin/` e `traefik.` para origens fora do Brasil ou com threat_score > 20). Especificado o contrato de governança `robots.txt` e automação declarativa via API v4 (ADR 011). Homologado por unanimidade (4/4).
- **[21/09/2026 - Sub-etapa 3.2.5]:** **Conclusão formal e homologação da Micro-Fase 3.2 (Segurança Perimétrica: WAF, Rate Limiting & Proteção DDoS):** Todos os 4 pilares de defesa de borda foram plenamente especificados, testados e aprovados por unanimidade pelos 4 subagentes especialistas (`arquiteto_solucoes`, `engenheiro_devops`, `engenheiro_backend`, `engenheiro_ia`). A camada de borda está 100% blindada contra OWASP Top 10, scanners hostis, ataques de força bruta, inundações volumétricas L3/L4/L7 e extração de dados por crawlers de IA, em estrita conformidade com a ADR 011 (Paradigma API-First & Operação Headless). Transição autorizada para a **Micro-Fase 3.3 (Arquitetura Criptográfica SSL/TLS Ponta a Ponta & ADR 010)**.
- **[21/09/2026 - Sub-etapa 3.3.1]:** Análise Comparativa de Modos SSL/TLS da Cloudflare concluída em `docs/analise_modos_ssl_cloudflare.md`. Desconstruída a falácia do modo Flexible (anti-padrão de falsa segurança que expõe a perna Cloudflare-VPS em HTTP puro porta 80, gerando risco severo de MitM, vazamento de credenciais e loop de redirecionamento 301 `ERR_TOO_MANY_REDIRECTS`). Analisado o modo Full e justificada a escolha obrigatória do modo Full (Strict) com validação de certificados Cloudflare Origin CA na porta 443 do Traefik. Documentada automação declarativa via API v4 (`PATCH /zones/{zone_id}/settings/ssl` com `value: "strict"`) em conformidade com a ADR 011. Homologado por unanimidade (4/4).
- **[21/09/2026 - Sub-etapa 3.3.2]:** **Formalização da Decisão Arquitetural ADR 010 (Obrigatoriedade de SSL/TLS Full Strict Ponta a Ponta):** Documento canônico consolidado em `docs/adr_010_obrigatoriedade_ssl_tls_full_strict.md` e incorporado formalmente como o 5º pilar de segurança em `ARQUITETURA.md`. Estabelecido o mandato corporativo inegociável que proíbe os modos Off, Flexible e Full, impondo exclusivamente o modo Full (Strict) com certificados de origem válidos na porta 443 do Traefik Ingress (Cloudflare Origin CA como primário e Let's Encrypt como fallback), cifragem mínima TLS 1.2 com TLS 1.3 como padrão prioritário, flags seguras no Django (`SESSION_COOKIE_SECURE`, `CSRF_COOKIE_SECURE`, `SECURE_PROXY_SSL_HEADER`) e script de auto-remediação via API v4 (ADR 011). Homologado por unanimidade (4/4).
- **[21/09/2026 - Sub-etapa 3.3.3]:** Especificação de Protocolos Criptográficos, Cifras Modernas (TLS 1.3) e HTTP/3 (QUIC) concluída em `docs/protocolos_cifras_tls_http3.md`. Fixada a versão mínima `min_tls_version = "1.2"`, banindo formalmente protocolos legados (SSLv3, TLS 1.0 e TLS 1.1). Ativado TLS 1.3 prioritário com handshake de 1-RTT, sigilo de encaminhamento perfeito (ECDHE) e cifras autenticadas AEAD (AES-GCM e ChaCha20-Poly1305). Ativado HTTP/3 baseado no protocolo QUIC sobre UDP 443, eliminando Head-of-Line blocking e viabilizando Connection Migration contínua para dispositivos móveis em redes 4G/5G. Ativado 0-RTT Connection Resumption com salvaguarda de segurança anti-replay restrita a métodos idempotentes (`GET`/`HEAD`). Mapeado script consolidado de provisionamento via API v4 (ADR 011). Homologado por unanimidade (4/4).
- **[21/09/2026 - Sub-etapa 3.3.4]:** Especificação de HTTP Strict Transport Security (HSTS) e Redirecionamento Automático HTTPS concluída em `docs/hsts_redirecionamento_https_cloudflare.md`. Configurado o redirecionamento incondicional na borda Anycast (`Always Use HTTPS`), convertendo HTTP 80 em HTTPS 443 com zero consumo de recursos na VPS Hostinger. Especificado o cabeçalho HSTS (RFC 6797) de padrão ouro: `max-age=31536000` (1 ano), `includeSubDomains` (protegendo todos os subdomínios presentes e futuros), `preload` (autorizando inclusão estática nos binários do Chrome, Edge, Safari e Firefox) e `nosniff`. Desenhada a estratégia de rollout seguro em 3 fases (5 min -> 24h -> 1 ano) e neutralizado o vetor de ataque SSL Stripping. Mapeado script de automação headless via Cloudflare API v4 (ADR 011). Homologado por unanimidade (4/4).
- **[21/09/2026 - Sub-etapa 3.3.5]:** **Conclusão formal e homologação da Micro-Fase 3.3 (Arquitetura Criptográfica SSL/TLS Ponta a Ponta & ADR 010):** Todos os 4 componentes criptográficos foram plenamente consolidados, auditados e homologados pelos 4 subagentes especialistas (`arquiteto_solucoes`, `engenheiro_devops`, `engenheiro_backend`, `engenheiro_ia`). O ecossistema SCSI opera com padrão de excelência de segurança bancária (Qualys SSL Labs Grade A+), combinando Full Strict mandatório (ADR 010), TLS 1.3, HTTP/3 (QUIC sobre UDP 443), Always Use HTTPS e HSTS Preload de 1 ano, em estrita aderência à ADR 011 (API-First & Operação Headless). Transição formalmente autorizada para a **Micro-Fase 3.4 (Integração Criptográfica da Origem: Cloudflare Origin CA vs ACME Traefik)**.
- **[21/09/2026 - Sub-etapa 3.4.1]:** Avaliação de Trade-offs entre Cloudflare Origin CA e Traefik ACME (Let's Encrypt) concluída em `docs/tradeoffs_origin_ca_vs_acme_traefik.md`. Definido por unanimidade o Cloudflare Origin CA com validade de 15 anos (5475 dias) como o padrão primário de produção do SCSI, eliminando riscos operacionais de queda por falha em renovações trimestrais, dispensando tokens com permissão de escrita de DNS dentro da VPS e reforçando a blindagem contra acesso direto ao IP da VPS (Direct Origin Access). Mantido o Let's Encrypt ACME via DNS-01 como padrão alternativo de contingência. Mapeado script de emissão automatizada via API v4 (`POST /client/v4/certificates`) aderente à ADR 011. Homologado por unanimidade (4/4).
- **[21/09/2026 - Sub-etapa 3.4.2]:** Especificação de Blindagem da Origem e Restrição de IPs da Cloudflare concluída em `docs/blindagem_origem_restricao_ips_cloudflare.md`. Mapeada a neutralização definitiva do vetor de ataque Direct Origin Bypass através da doutrina de Defesa em Profundidade (ADR 008) em 2 níveis: Nível 1 no Firewall UFW (Kernel Linux/iptables) bloqueando a entrada geral e liberando as portas 80/443 estritamente para os 15 prefixos IPv4 e 7 prefixos IPv6 oficiais da Cloudflare; e Nível 2 no Traefik Ingress com `trustedIPs` e `ipAllowList`, garantindo que apenas tráfego vindo da borda seja processado e que o cabeçalho `CF-Connecting-IP` seja confiável para o Django. Mapeado script de sincronização e auditoria dinâmica de IPs via API oficial (`GET /client/v4/ips`) conforme a ADR 011.
- **[24/09/2026 - Sub-etapa 3.4.3]:** Modelagem Declarativa dos Ingressos do Traefik para Roteamento TLS concluída em `docs/modelagem_ingress_traefik_tls.md` (0 bytes CR — ADR 007). Especificados: (1) Configuração estática `traefik.yml` com entrypoints `web:80` (redirect HTTPS) e `websecure:443` (TLS + trustedIPs dos 15 IPv4 e 7 IPv6 da Cloudflare) e timeouts customizados para IA; (2) Configuração dinâmica `dynamic_tls.yml` carregando o par Cloudflare Origin CA via docker secrets, com cifras AEAD modernas e curva X25519; (3) Middlewares corporativos `dynamic_middlewares.yml` com compressão (excluindo `text/event-stream` para SSE), HSTS, BasicAuth bcrypt + IPAllowList; (4) Labels Docker Swarm declarativas. Documento revisado e **homologado colegiadamente** (4/4 aprovações com correção de ressalvas de sintaxe do Traefik v3).
- **[24/09/2026 - Sub-etapa 3.4.4]:** **Conclusão formal e homologação da Micro-Fase 3.4 (Integração Criptográfica da Origem):** A transição do tráfego criptografado entre a Cloudflare e a VPS foi perfeitamente mapeada utilizando Origin CA (15 anos), eliminando a vulnerabilidade de exposição direta de IP e assegurando que os containers recebam os cabeçalhos de proxy confiavelmente validados. Transição formalmente autorizada para a **Micro-Fase 3.5 (Geração do Documento Executivo e Commit/Push)**.
- **[24/09/2026 - Micro-Fase 3.5]:** Geração do Relatório Executivo em PDF (`Passo_3_Cloudflare_e_Passo_4_SSL_TLS.pdf`) contendo o resumo estratégico da blindagem de borda, WAF, mitigação DDoS e criptografia Full Strict. Todos os 18 arquivos Markdown gerados durante a Fase 3 e os PDFs executivos foram comitados (commit `2d940d7`) e submetidos ao repositório remoto via `git push origin main`.
- **[24/09/2026 - ENCERRAMENTO DA FASE 3]:** A infraestrutura de borda (Cloudflare) e o modelo de ingresso TLS (Traefik) foram consolidados com selo de segurança corporativa (Grade A+). A arquitetura está pronta para receber o SO e a orquestração de containers. Início formal da **Fase 4: Infraestrutura da VPS & Hardening Linux**.
- **[24/09/2026 - Sub-etapa 4.1]:** Análise e Seleção de Sistema Operacional & Hardening Base documentados em `docs/analise_sistema_operacional_hardening.md` (0 bytes CR — ADR 007). Firmada a **ADR 012**, estabelecendo o **Ubuntu 24.04 LTS (Noble Numbat)** como SO padrão da arquitetura SCSI em virtude do Kernel 6.8 (ótimo suporte a rede Docker e eBPF) e ciclo de vida estendido (2029+). Mapeadas as rotinas operacionais de base: configuração de timezone `UTC`, sincronização crítica via `systemd-timesyncd`, mitigação proativa com `unattended-upgrades`, purga completa do `snapd` para redução de superfície de ataque/consumo de recursos e consolidação do AppArmor em modo enforce. Homologado colegiadamente.
- **[24/09/2026 - Sub-etapa 4.2]:** Especificação de Segurança de Acesso SSH documentada em `docs/especificacao_acesso_ssh_hardening.md` (0 bytes CR — ADR 007). Definido o uso exclusivo de chaves de curva elíptica **Ed25519** em substituição ao RSA/Senhas. Estabelecidas regras restritivas no `sshd_config` operando sob o conceito Zero Trust: bloqueio absoluto do usuário root (`PermitRootLogin no`), desativação de senhas (`PasswordAuthentication no`), ofuscação de porta para mitigar scanners passivos (ex: `Port 22444`), limitação de sessões inativas (`ClientAliveInterval`) e restrição de acesso a usuários específicos via `AllowUsers`. Mapeado o procedimento seguro (fail-safe) de aplicação sem risco de lockout.
- **[24/09/2026 - Sub-etapa 4.3]:** Especificação do Firewall Nativo (UFW) e Tratamento de Roteamento Docker registrada em `docs/firewall_ufw_cloudflare_docker.md` (0 bytes CR — ADR 007). Estruturada a política Base Default Deny (bloquear tudo) e Default Allow Outgoing. Mapeada a abertura da porta 22444/tcp (SSH customizado) e o bloqueio estrito das portas 80/443 para aceitarem conexões apenas dos 15 blocos IPv4 e 7 blocos IPv6 oficiais da Cloudflare, concretizando o Nível 1 da ADR 008. Resolvido o grave problema de bypass do UFW pelo Docker (via iptables PREROUTING) injetando um drop chain explícito na regra `DOCKER-USER` no arquivo `/etc/ufw/after.rules`, forçando o Swarm a respeitar as restrições da nuvem externa.
- **[24/09/2026 - Sub-etapa 4.4]:** Otimização de Kernel (Sysctl) e Tuning de Alta Performance arquitetados em `docs/kernel_sysctl_tuning_performance.md` (0 bytes CR — ADR 007). Otimizações estruturadas para suportar alta carga de I/O, conexões TCP (Traefik) e payloads intensivos de IA: ativado algoritmo `tcp_bbr` e escalonador `fq`; elevado `somaxconn` para 65535; ampliação de descritores de arquivo (`fs.file-max`) para 2M; redefinição de janelas de TCP buffers (até 16MB) e ajuste do `swappiness` para 10, preservando estabilidade em workloads de memória agressivos (PostgreSQL/Redis).
- **[24/09/2026 - Micro-Fase 4.5]:** Geração do Relatório Executivo em PDF (`Fase_4_Infraestrutura_VPS_Hardening.pdf`) consolidando as fundações operacionais, SSH Zero Trust, Firewall restrito à Cloudflare e Kernel BBR. Todos os 4 documentos de engenharia e o PDF executivo foram comitados (commit `bba0a8c`) e submetidos ao repositório remoto via `git push origin main`.
- **[24/09/2026 - ENCERRAMENTO DA FASE 4]:** A VPS Hostinger está agora sob blindagem grau militar, com superfície de ataque mínima e Kernel maximizado para I/O massivo. Início formal da **Fase 5: Orquestração de Containers (Docker Swarm) e Ingressos Traefik**.
- **[24/09/2026 - Sub-etapa 5.1]:** Topologia de Redes Overlay e Isolamento especificados em `docs/topologia_redes_swarm.md` (0 bytes CR — ADR 007). Desenhada a arquitetura de micro-segmentação (Zero Trust interno) que quebra a rede do cluster em três domínios estanques: `scsi_public` (Tráfego HTTP/Ingress), `scsi_internal` (Lógica e AMQP) e `scsi_data` (Persistência relacional e em memória). Fica garantido que o Traefik (Edge) não possui rota física ou lógica para se comunicar com o PostgreSQL/Redis, mitigando o alcance de eventuais invasões L7. Sub-etapa **Homologada Colegiadamente** (4/4) após correção solicitada pelo DevOps, incluindo a ressalva documentada sobre o trade-off de rede da flag `--opt encrypted` (IPSec vs VPC).
- **[24/09/2026 - Sub-etapa 5.2]:** Protocolo de Injeção de Segredos mapeado em `docs/gestao_segredos_docker_swarm.md` (0 bytes CR — ADR 007). Estabelecida a proibição sumária do uso de arquivos `.env` ou diretivas `environment:` para passagem de credenciais em texto plano. Toda informação sensível (Chaves da OpenAI, Certificados Origin CA, Senha do PostgreSQL) será provisionada via **Docker Secrets**. Definido que os segredos fluirão criptografados via mTLS e Raft, sendo montados apenas em memória RAM (`tmpfs` em `/run/secrets/`), blindando a aplicação contra vazamentos por `docker inspect` ou persistência em disco. Mapeado protocolo de imutabilidade e rotação via Sufixo-V (`_v2`).
- **[24/09/2026 - Sub-etapa 5.3]:** Implantação Core do Traefik Ingress e Socket Proxy especificada em `docs/implantacao_traefik_socket_proxy.md` (0 bytes CR — ADR 007). Desenhado o blueprint do `docker-compose.traefik.yml`. Solucionada a vulnerabilidade crítica de escalonamento de privilégio do Traefik isolando o `/var/run/docker.sock` através do container `tecnativa/docker-socket-proxy`. O proxy provê acesso estritamente Read-Only às APIs do Docker limitadas a redes e serviços. Sub-etapa **Homologada Colegiadamente** (4/4) com ajustes no deploy: definido o bind de portas em `mode: host` para bypassar a malha Ingress (preservando IP real Cloudflare e SSE para IA) acompanhado de `mode: global` sobre os Managers, garantindo Alta Disponibilidade (HA) sem travar a alocação de rede.
- **[24/09/2026 - Sub-etapa 5.4]:** Políticas de Deploy, Escalabilidade e Resiliência arquitetadas em `docs/politicas_resiliencia_swarm.md` (0 bytes CR — ADR 007). Estabelecido o padrão corporativo de *Cgroups* para prevenção de OOM (*Memory Leaks* ou loops de IA não derrubarão o servidor graças aos *Hard Limits* de recursos). Configurada a rotina de Rolling Updates (`update_config: start-first`) garantindo Zero-Downtime e Rollback automático em caso de falha de novas versões. Implementados *Healthchecks* HTTP e rotinas de *Self-Healing* (`restart_policy`) que recriam autonomamente a infraestrutura degradada.
- **[24/09/2026 - Micro-Fase 5.5]:** Geração do Relatório Executivo em PDF (`Fase_5_Orquestracao_Docker_Swarm.pdf`) documentando as decisões arquiteturais da camada de orquestração: Micro-segmentação L7, Docker Secrets (tmpfs), Traefik + Socket Proxy, e Cotas de Resiliência. Todos os 4 documentos de engenharia da Fase 5 e o PDF executivo foram comitados (commit `9f5d7f1`) e enviados ao GitHub com sucesso.
- **[24/09/2026 - ENCERRAMENTO DA FASE 5]:** A fundação do cluster Docker Swarm está arquitetada com padrões de nível enterprise (Zero-Trust, Zero-Downtime, Self-Healing). Início formal da **Fase 6: Core Web e Persistência de Dados (PostgreSQL e Redis)**.
- **[24/09/2026 - Sub-etapa 6.1]:** Especificação arquitetural do Banco de Dados Relacional em `docs/arquitetura_postgresql_16.md` (0 bytes CR — ADR 007). Definido o uso da imagem oficial `pgvector/pgvector:pg16` para garantir suporte futuro a embeddings de IA (Fase 7). Isolamento de rede garantido (`scsi_data`), persistência física via volumes nomeados vinculados aos *Managers* e ingestão de credenciais puramente via *Docker Secrets* (sufixo `_FILE` no PostgreSQL). Sub-etapa **Homologada Colegiadamente** (4/4) após ajustes críticos: (1) Alteração de `placement` para `node.labels.db_node == "true"` impedindo orfandade do volume em clusters multi-manager e (2) aumento da reserva de memória para 2GB visando segurar índices HNSW sem swap em disco.
- **[24/09/2026 - Sub-etapa 6.2]:** Especificação arquitetural do Banco de Dados em Memória em `docs/arquitetura_redis.md` (0 bytes CR — ADR 007). Escolhida a imagem `redis:7-alpine` (~15MB) por segurança e eficiência. Contorno técnico desenhado para ingerir *Docker Secrets* sem suporte nativo a `_FILE`, utilizando leitura dinâmica de shell (`$$(cat /run/secrets/...)`) no entrypoint. Multihoming de rede implementado (`scsi_data` e `scsi_internal`) para permitir comunicação fluida entre Django (cache/sessão) e Celery (result backend). Habilitada persistência AOF (`--appendonly yes`) para preservação de sessões contra reboots temporários. Sub-etapa **Homologada Colegiadamente** (4/4) com dois *hotfixes* cruciais: (1) Remoção da flag `--requirepass` visível do comando, gerando o arquivo `/tmp/redis.conf` dinamicamente para não vazar a senha no `ps` do host e (2) Ajuste de teto de memória (`limits: 2048M` no Swarm vs `--maxmemory 1500mb` com política `volatile-lru` interna no Redis) impedindo que OOM Killer derrubasse o serviço, prejudicando os retornos do LangGraph.
- **[24/09/2026 - Sub-etapa 6.3]:** Estruturação corporativa do framework Web em `docs/arquitetura_base_django.md` (0 bytes CR — ADR 007). Abandono do monólito padrão `settings.py` em favor de uma arquitetura modular (`base.py`, `local.py`, `production.py`) inspirada no Cookiecutter. Sub-etapa **Homologada Colegiadamente** (4/4) com ajustes arquiteturais pesados voltados para IA: (1) Substituição sumária do padrão WSGI pelo **ASGI (Gunicorn + Uvicorn Workers)** para suportar *Streaming* de tokens via Server-Sent Events (SSE) e conexões longas não-bloqueantes. (2) Injeção do bloco de `healthcheck` no Swarm, requisito obrigatório apontado pelo DevOps para que o *Rolling Update* (Zero-Downtime) funcione corretamente. (3) Adição das labels `entrypoints=websecure` e `tls=true` garantindo criptografia forçada no Traefik Ingress. Mapeamento de topologia do Django finalizado atuando como eixo central pelas três redes (`scsi_public`, `scsi_data`, `scsi_internal`).
- **[24/09/2026 - Sub-etapa 6.4]:** Especificação de Build Otimizado em `docs/arquitetura_dockerfile_django.md` (0 bytes CR — ADR 007). Desenho de imagem baseada no padrão *Multi-stage Build*, separando o ambiente de compilação pesado (gcc, headers, pacotes C-extension para IA/PostgreSQL) da imagem limpa de Produção. Decisão arquitetural de suprimir a imagem Alpine para o Python em favor do `debian-slim` visando evitar quebras de compilação C nos *wheels* do ecossistema de ML. Aceleração da montagem orquestrada pelo gerenciador `uv` (ADR 009) e reforço de blindagem com a supressão do root (`USER appuser`). Sub-etapa **Homologada Colegiadamente** (4/4) após correção crucial de permissão apontada pela infraestrutura: O diretório `/app` recebia o ownership do root pelo `WORKDIR`, causando `PermissionError` futuro ao gravar `staticfiles` ou `media`. Adicionado o comando `chown -R appuser:appgroup /app` antes do drop de privilégios. O contêiner já nasce programado para subir a camada ASGI/Uvicorn definida na 6.3.
- **[24/09/2026 - Micro-Fase 6.5]:** Geração e consolidação do Relatório Executivo em PDF (`Fase_6_Core_Web_e_Persistencia.pdf`), validando todas as aprovações técnicas e salvando os artefatos documentais.
- **[24/09/2026 - ENCERRAMENTO DA FASE 6]:** O núcleo transacional e a estrutura base da API Web estão finalizados, desacoplados e performáticos. Início formal da **Fase 7: Mensageria Assíncrona & IA Cognitiva (RabbitMQ, Celery e LangGraph)**.
- **[24/09/2026 - Sub-etapa 7.1 (Passo 12)]:** Especificação arquitetural do Broker de Mensagens em `docs/arquitetura_rabbitmq.md` (0 bytes CR — ADR 007). Escolhida a imagem `rabbitmq:3-management-alpine` garantindo acesso à REST API de métricas para consumo sistêmico pelo Antigravity (ADR 011). Reforço de segurança utilizando a injeção nativa de segredos na RAM via sufixo `_FILE` (`RABBITMQ_DEFAULT_USER_FILE`), suprimindo vazamentos. Isolamento perimetral consolidado pela restrição de anexação única e exclusiva à rede `scsi_internal`, blindando o acesso e garantindo que apenas Django e Celery comuniquem-se com o broker. Volume fixado via *label constraint* (`mq_node`) preservando a integridade das filas (Mnesia) em caso de reinício. Sub-etapa **Homologada Colegiadamente** (4/4) após os seguintes hotfixes: (1) Inclusão obrigatória de bloco `healthcheck` (`rabbitmq-diagnostics -q ping`) para prevenir Crash Loops no Celery e (2) Padronização do *Payload Magro* ditada pela IA (enviar apenas ponteiros e IDs no lugar de dados longos para respeitar o teto de 1024M de RAM imposto).
- **[24/09/2026 - Sub-etapa 7.2 (Passo 13)]:** Especificação arquitetural dos Operários de Background em `docs/arquitetura_celery_worker.md` (0 bytes CR — ADR 007). Aplicação do princípio DRY no Swarm: o Celery reutilizará exatamente a mesma imagem Multi-stage do Django (Passo 9), garantindo paridade de código e ORM. Topologia de rede bifurcada aprovada: atachado em `scsi_internal` (para ler RabbitMQ e gravar no Redis) e em `scsi_data` (para cumprir o contrato do Payload Magro e extrair os embeddings direto do PostgreSQL). Otimização crucial para cargas de Inteligência Artificial Generativa: substituição do pool de processos nativo (Prefork) por *Threads* (`--pool=threads --concurrency=10`), alinhando-se ao perfil I/O Bound das chamadas de rede da OpenAI/Anthropic, reduzindo dramaticamente o footprint de RAM e permitindo alta densidade de concorrência. Sub-etapa **Homologada Colegiadamente** (4/4) após ajustes vitais: (1) Inclusão do roteamento rígido `-Q ia_tasks` para impedir que a arquitetura otimizada por threads consuma tarefas *CPU Bound*, (2) Injeção do liveness probe (Healthcheck `celery inspect ping`) para matar containers em "Zombie State", (3) Elevação do limite para 1024M prevenindo OOM Kill com JSONs grandes e (4) Veto formal à ferramenta `gevent` devido a deadlocks documentados com gRPC e LangChain.
- **[24/09/2026 - Sub-etapa 7.3 (Passo 14)]:** Especificação arquitetural do Orquestrador Temporal em `docs/arquitetura_celery_beat.md` (0 bytes CR — ADR 007). Consolidação do uso do agendador centralizado suportado pelo `django-celery-beat` (DatabaseScheduler), transferindo a fonte da verdade dos *crontabs* do código-fonte para o PostgreSQL (`scsi_data`), viabilizando edições dinâmicas sem rebuild do container. Implementação rígida do padrão *Singleton* arquitetural limitando estritamente a implantação a 1 única réplica (`deploy.replicas: 1`), e constraint no `manager`, evitando loops de replicação e disparos duplicados de tarefas como limpeza de sessões ou otimizações vectoriais do BD. Footprint de recursos fixado preventivamente em patamares baixíssimos (256M RAM / 0.2 CPU) dada a natureza computacional nula do envio de pacotes ao RabbitMQ. Sub-etapa **Homologada Colegiadamente** (4/4) incorporando ressalvas vitais: (1) Omissão proposital do `healthcheck` validada pelo DevOps (o *Beat* não possui comando ping e usá-lo causaria falsos *crash loops*), (2) Exigência de paridade de Fuso Horário no `settings.py` (`CELERY_ENABLE_UTC=False`) para evitar que disparos agendados no admin web ocorram dessincronizados e (3) Correção do escopo das rotinas de banco (a IA apontou que o `VACUUM` diário no *pgvector* só se justifica como rotina `REINDEX` noturna forçada em casos de ingestão massiva em lote para superar o *autovacuum* nativo).
- **[24/09/2026 - Sub-etapa 7.4 (Passo 15)]:** Especificação arquitetural do Orquestrador Cognitivo de IA Local em `docs/arquitetura_langgraph_ollama.md` (0 bytes CR — ADR 007). Decisão estratégica de soberania absoluta de dados (Zero-Leak) abdicando de APIs externas (OpenAI) em favor da conteinerização do **Ollama** diretamente no Docker Swarm. Imagem ancorada exclusivamente na rede `scsi_internal` sem portas externas. Definição rígida de *Memory Fencing* (`limits.memory: 6G`) para proteger o PostgreSQL e o Django do temido OOM Kill durante a inferência por CPU de modelos quantizados. Consolidação do fluxo LangGraph sendo roteado a partir da RAM do Celery Worker (Sub-etapa 7.2) para os nós do banco vetorial e API REST local do Ollama (`ChatOllama` / `OllamaEmbeddings`). Stack de modelos eleita: `llama3.1` para geração conversacional e `nomic-embed-text` para as matrizes do *pgvector*. Sub-etapa **Homologada Colegiadamente** (4/4) com intervenções vitais de *capacity planning*: (1) Redução do limite de RAM de 6GB para 4GB (o DevOps alertou que 6GB arriscaria as instâncias de 8GB de RAM), (2) Injeção de `OLLAMA_KEEP_ALIVE=60s` para evitar vazamento contínuo de memória após inatividade, (3) Adequação do motor cognitivo para modelos "Sub-4B" (ex: **`llama3.2`** e **`qwen2.5`**) para garantir velocidade de CPU e farta sobra de RAM para o Context Window (*KV Cache* do RAG), e (4) Alerta operacional sobre a ativação obrigatória da label `ai_node` via CLI para tirar o container de estado *Pending*.
- **[24/09/2026 - ENCERRAMENTO DA FASE 7]:** A infraestrutura completa de processamento assíncrono e Inteligência Artificial Local (Soberana) foi definida, documentada e aprovada pelo conselho arquitetural.






















